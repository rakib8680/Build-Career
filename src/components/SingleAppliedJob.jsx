import React from 'react';

const SingleAppliedJob = ({appliedJob}) => {
    console.log(appliedJob)
    const {company, logo, position, type, worktime, location , salary} = appliedJob
    return (
        <div>
            
        </div>
    );
};

export default SingleAppliedJob;